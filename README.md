## Windbnp

Windbnb est une sorte de petite version de Airbnb. On peut rechercher par nom de ville, avec gestion des villes non existantes ou sans logement. Le tout développé sous `webpack` en `HTML`, `CSS`, et `JavaScript`. Les données proviennent d'un fichier `JSON`.

<img src = "src/images/screenshot.png"></img>
